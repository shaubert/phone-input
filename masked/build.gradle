apply plugin: 'com.android.library'

dependencies {
    api project(':library')
    implementation('com.shaubert.maskedinput:library:1.3')
}

android {
    compileSdkVersion 29

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 29
    }
}

apply from: '../maven_push.gradle'