apply plugin: 'com.android.library'

dependencies {
    compile project(':masked')
    compile 'com.shaubert.maskedinput:met-extension:1.2.7'
}

android {
    compileSdkVersion 25
    buildToolsVersion '25.0.2'

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 25
    }
}

apply from: '../maven_push.gradle'